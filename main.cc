import <iostream>;

const int Rows = 25;
const int Col = 79;

void playgame() {

    // character
    bool attach_merchant;
    for(int i = 1; i<=5;i++) {
        //prepare map here
        //update character postion
        //generate enemy gold poition if map not given
        
        while(true){
            // user move
                // check enemy
                // check gold
                // check dragon gold
            // user attack
                // check user die ? enemy die ?
                // if enemy die -> collect gold
            // user drink portion
            if user goes to stairs
                break;
        }
    }
}

int main() {
    char *Graph[25];
    for (int i = 0; i < 25; i++) {
        Graph[i] = new char[Col];
        getline(cin, source);

    }

}

// 1. combat function
// 2. drink portion
// 3. converting function graph -> Displaying graph
// 4. parsing function (read in the txt -> graph)
// 5. creation function (generate empty graph)
// 6. generation (character , portion, gold, dragon, enemy, staircase)
// 7. play game function