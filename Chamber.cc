class Chamber {
        int row_min;
        int row_max;
        int col_min[];
        int col_max[];
    public:
        void generate(Character &Charc) {
        ... make some generation
        while(...)
        // if generated successfully
        Charc.x = x;
        Charc.y = y;
        }
}